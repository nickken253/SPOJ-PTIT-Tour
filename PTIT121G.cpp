#include <bits/stdc++.h>
using namespace std;
#define faster                   \
    ios::sync_with_stdio(false); \
    cin.tie(0);                  \
    cout.tie(0);
#define endl "\n"
#define ull unsigned long long
#define ll long long


//NOT_DONE_______________
int main()
{
    faster;
    int n;
    do{
        cin >> n;
        if(n == 0) break;
        int a[n][n];
        vector<int> N1;
        vector<int> N2;
        for(int i = 0; i < n; i ++){
            for(int j = 0; j < n; j++){
                cin >> a[i][j];
            }
        }
        for(int i = 0; i <)
    }
    while(true);
}
